<!DOCTYPE html>
<html lang="bn">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>✦ AI স্টাডি প্ল্যানার — ২য় মাসিক পরীক্ষা</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Tiro+Bangla:ital@0;1&family=Hind+Siliguri:wght@400;500;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg1:#0b0d12; --bg2:#12151d; --bg3:#171b25;
    --card:#1b1f2a; --card2:#20242f; --line:#2a2f3d;
    --text:#eef0f4; --muted:#8b93a5; --muted2:#666e80;
    --gold:#d8b76a; --gold-soft:rgba(216,183,106,0.14); --gold-line:rgba(216,183,106,0.35);
    --emerald:#3ab486; --rose:#d97a86; --sapphire:#6f9fd8; --amber:#d9a256; --violet:#a690d8; --teal:#4fb8ae; --coral:#d88f6f;
    --danger:#d9636b; --radius:16px;
    --shadow: 0 4px 16px rgba(0,0,0,0.35);
    --shadow-sm: 0 2px 8px rgba(0,0,0,0.25);
  }
  body.light{
    --bg1:#f4f1ea; --bg2:#faf8f3; --bg3:#ffffff;
    --card:#ffffff; --card2:#f6f3ea; --line:#e6e0d2;
    --text:#211f1b; --muted:#6b6558; --muted2:#8b8576;
    --gold:#a9822f; --gold-soft:rgba(169,130,47,0.1); --gold-line:rgba(169,130,47,0.3);
    --shadow: 0 3px 12px rgba(30,25,10,0.1);
    --shadow-sm: 0 1px 6px rgba(30,25,10,0.08);
  }
  body.theme-emerald{
    --bg1:#081512; --bg2:#0d1f19; --bg3:#122a22;
    --card:#10231c; --card2:#152c23; --line:#1f3a30;
    --text:#eaf5ee; --muted:#88a89a; --muted2:#5c7a6c;
    --gold:#3fceac; --gold-soft:rgba(63,206,172,0.14); --gold-line:rgba(63,206,172,0.35);
    --shadow: 0 4px 16px rgba(0,0,0,0.4); --shadow-sm: 0 2px 8px rgba(0,0,0,0.28);
  }
  body.theme-ocean{
    --bg1:#050f1a; --bg2:#0a1826; --bg3:#0e2233;
    --card:#0c1e2e; --card2:#123049; --line:#1d3f57;
    --text:#e9f4fb; --muted:#82a6bd; --muted2:#597b90;
    --gold:#4fc3e8; --gold-soft:rgba(79,195,232,0.14); --gold-line:rgba(79,195,232,0.35);
    --shadow: 0 4px 16px rgba(0,0,0,0.4); --shadow-sm: 0 2px 8px rgba(0,0,0,0.28);
  }
  body.theme-rose{
    --bg1:#160a14; --bg2:#20101c; --bg3:#2b1725;
    --card:#231320; --card2:#2c1928; --line:#3d2536;
    --text:#f7ecf3; --muted:#b18ba8; --muted2:#7c5c73;
    --gold:#e88fb0; --gold-soft:rgba(232,143,176,0.14); --gold-line:rgba(232,143,176,0.35);
    --shadow: 0 4px 16px rgba(0,0,0,0.4); --shadow-sm: 0 2px 8px rgba(0,0,0,0.28);
  }
  *{box-sizing:border-box;-webkit-tap-highlight-color:transparent;}
  html,body{margin:0;padding:0;min-height:100%;}
  body{
    font-family:'Hind Siliguri','Noto Sans Bengali',sans-serif;
    color:var(--text);
    background:linear-gradient(160deg, var(--bg1), var(--bg2) 55%, var(--bg3));
    min-height:100vh; overflow-x:hidden;
  }
  h1,h2,h3,.display{font-family:'Tiro Bangla','Hind Siliguri',serif;}
  ::-webkit-scrollbar{width:6px;} ::-webkit-scrollbar-thumb{background:rgba(216,183,106,0.3);border-radius:10px;}

  /* ---------- Splash ---------- */
  #splash{position:fixed;inset:0;z-index:999;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:16px;
    background:linear-gradient(160deg, var(--bg1), var(--bg2) 60%, var(--bg3));transition:opacity .5s,visibility .5s;}
  #splash.hide{opacity:0;visibility:hidden;pointer-events:none;}
  .splash-logo{width:80px;height:80px;border-radius:20px;display:flex;align-items:center;justify-content:center;font-size:36px;
    background:var(--card);border:1px solid var(--gold-line);box-shadow:var(--shadow);animation:breathe 2s ease-in-out infinite;}
  @keyframes breathe{0%,100%{transform:scale(1);opacity:1;}50%{transform:scale(1.05);opacity:.85;}}
  .splash-title{font-family:'Tiro Bangla',serif;font-weight:400;font-size:20px;color:var(--gold);letter-spacing:.5px;}
  .splash-bar{width:140px;height:3px;border-radius:10px;background:rgba(216,183,106,0.15);overflow:hidden;}
  .splash-bar span{display:block;height:100%;width:35%;border-radius:10px;background:var(--gold);animation:loadmove 1s ease-in-out infinite;}
  @keyframes loadmove{0%{margin-left:-40%;}100%{margin-left:100%;}}

  #app{max-width:520px;margin:0 auto;padding-bottom:90px;position:relative;}

  /* ---------- Header ---------- */
  header.top{position:sticky;top:0;z-index:50;padding:16px 16px 8px;
    background:linear-gradient(180deg, var(--bg1) 65%, transparent);}
  .top-row{display:flex;align-items:center;justify-content:space-between;gap:10px;}
  .brand{display:flex;align-items:center;gap:11px;}
  .brand .logo{width:42px;height:42px;border-radius:12px;background:var(--card);border:1px solid var(--gold-line);
    display:flex;align-items:center;justify-content:center;font-size:19px;color:var(--gold);}
  .brand h1{font-family:'Tiro Bangla',serif;font-weight:400;font-size:16.5px;margin:0;color:var(--text);letter-spacing:.2px;}
  .brand p{margin:0;font-size:10.5px;color:var(--muted);letter-spacing:.3px;}
  .icon-btn{width:36px;height:36px;border-radius:11px;border:1px solid var(--line);background:var(--card);color:var(--text);
    display:flex;align-items:center;justify-content:center;font-size:15px;cursor:pointer;}
  .icon-btn:active{opacity:.7;}

  .exam-banner{margin-top:12px;padding:11px 14px;border-radius:14px;background:var(--gold-soft);border:1px solid var(--gold-line);
    color:var(--gold);font-size:11.5px;font-weight:600;display:flex;justify-content:space-between;align-items:center;}

  .header-stats{display:grid;grid-template-columns:repeat(4,1fr);gap:7px;margin-top:10px;}
  .stat-card{padding:9px 4px;text-align:center;background:var(--card);border:1px solid var(--line);border-radius:13px;}
  .stat-card .val{font-size:14px;font-weight:700;color:var(--text);}
  .stat-card .lab{font-size:8.5px;color:var(--muted);margin-top:2px;}

  .progress-wrap{margin-top:10px;padding:13px 15px;display:flex;align-items:center;gap:13px;background:var(--card);border:1px solid var(--line);border-radius:16px;}
  .ring{position:relative;width:52px;height:52px;flex:none;}
  .ring svg{transform:rotate(-90deg);}
  .ring .pct{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-size:11px;font-weight:700;color:var(--text);}
  .progress-info{flex:1;} .progress-info .t1{font-size:11px;color:var(--muted);}
  .progress-info .t2{font-size:13px;font-weight:600;margin-top:1px;color:var(--text);}
  .bar{height:6px;border-radius:8px;background:var(--card2);overflow:hidden;margin-top:7px;}
  .bar>div{height:100%;border-radius:8px;background:linear-gradient(90deg,var(--gold),var(--emerald));transition:width .8s ease;}

  .tabs{display:flex;gap:6px;padding:12px 16px 2px;}
  .tab-btn{flex:1;padding:9px 4px;text-align:center;border-radius:11px;border:1px solid var(--line);background:var(--card);
    color:var(--muted);font-size:11.5px;font-weight:600;cursor:pointer;}
  .tab-btn.active{background:var(--gold-soft);color:var(--gold);border-color:var(--gold-line);}

  .view{padding:8px 16px 10px;display:none;} .view.active{display:block;}

  .paper{background:var(--card);border:1px solid var(--line);border-radius:var(--radius);color:var(--text);}
  .quote-card{margin:10px 0;padding:14px 16px;position:relative;border-left:3px solid var(--gold);}
  .quote-card p{margin:0;font-size:13px;line-height:1.65;font-family:'Tiro Bangla',serif;color:var(--text);}

  .start-banner{padding:14px;text-align:center;margin-bottom:10px;border-left:3px solid var(--sapphire);}
  .start-banner b{font-size:13px;} .start-banner p{margin:4px 0 0;font-size:11px;color:var(--muted);}

  .day-nav{display:flex;align-items:center;justify-content:space-between;gap:10px;margin:12px 0;}
  .day-nav button{flex:1;padding:11px 8px;border-radius:12px;border:1px solid var(--line);background:var(--card);color:var(--text);font-size:12.5px;font-weight:600;cursor:pointer;font-family:inherit;}
  .day-nav button:disabled{opacity:.3;}
  .day-title{text-align:center;flex:1;}
  .day-title .dnum{display:inline-block;background:var(--gold-soft);border:1px solid var(--gold-line);color:var(--gold);
    font-size:10px;font-weight:700;padding:2px 11px;border-radius:20px;margin-bottom:4px;letter-spacing:.3px;}
  .day-title .d1{font-family:'Tiro Bangla',serif;font-size:15px;font-weight:400;color:var(--text);}
  .day-title .d2{font-size:10.5px;color:var(--muted);margin-top:1px;}
  .badges{display:flex;gap:5px;flex-wrap:wrap;justify-content:center;margin-top:6px;}
  .badge{font-size:9px;padding:3px 10px;border-radius:20px;font-weight:600;letter-spacing:.2px;}
  .badge.study{background:rgba(111,159,216,0.15);color:var(--sapphire);}
  .badge.holiday{background:rgba(58,180,134,0.15);color:var(--emerald);}
  .badge.final{background:rgba(217,122,134,0.15);color:var(--rose);}
  .badge.revision{background:rgba(166,144,216,0.15);color:var(--violet);}
  .badge.private{background:rgba(217,162,86,0.15);color:var(--amber);}
  .badge.today{background:var(--gold-soft);color:var(--gold);}
  .badge.hard{background:rgba(226,99,95,0.15);color:#e2635f;}

  .day-progress-ring{display:flex;justify-content:center;margin:10px 0 2px;}

  .session{margin-bottom:8px;padding:12px 13px;border-left:3px solid var(--gold);display:flex;gap:11px;align-items:flex-start;}
  .session.current{border-color:var(--emerald);background:var(--card2);}
  .session.done{opacity:.45;}
  .s-icon{width:34px;height:34px;border-radius:10px;background:var(--card2);display:flex;align-items:center;justify-content:center;font-size:15px;flex:none;}
  .s-body{flex:1;min-width:0;}
  .s-top{display:flex;justify-content:space-between;gap:6px;align-items:center;}
  .s-name{font-size:12.5px;font-weight:600;} .s-time{font-size:10px;color:var(--muted);white-space:nowrap;}
  .s-topic{font-size:11.5px;color:var(--muted);margin-top:2px;}
  .session.done .s-topic,.session.done .s-name{text-decoration:line-through;}
  .s-tags{display:flex;gap:5px;margin-top:5px;flex-wrap:wrap;}
  .chk{width:24px;height:24px;border-radius:8px;border:1px solid var(--line);flex:none;cursor:pointer;
    display:flex;align-items:center;justify-content:center;font-size:13px;color:transparent;background:var(--card2);}
  .chk.on{background:var(--emerald);color:#08281c;border-color:transparent;}
  .s-actions{display:flex;flex-direction:column;gap:6px;align-items:center;}
  .diffBtn{width:24px;height:24px;border-radius:8px;border:1px solid var(--line);background:var(--card2);
    display:flex;align-items:center;justify-content:center;font-size:11px;cursor:pointer;opacity:.35;}
  .diffBtn.on{opacity:1;border-color:var(--gold-line);background:var(--gold-soft);}
  @keyframes pop{0%{transform:scale(1);}50%{transform:scale(1.2);}100%{transform:scale(1);}}
  .chk.anim{animation:pop .25s ease;}

  .prayer-head{display:flex;justify-content:space-between;align-items:center;padding:12px 13px;cursor:pointer;}
  .prayer-head b{font-size:12px;font-weight:600;}
  .prayer-list{display:none;padding:0 13px 12px;}
  .prayer-list.open{display:grid;grid-template-columns:repeat(5,1fr);gap:5px;}
  .prayer-item{text-align:center;padding:7px 2px;border-radius:10px;background:var(--card2);}
  .prayer-item .pi{font-size:14px;} .prayer-item .pn{font-size:9px;color:var(--muted);margin-top:1px;}
  .prayer-item .pt{font-size:10px;font-weight:600;color:var(--text);}

  textarea.note{width:100%;min-height:56px;margin-top:9px;border-radius:11px;border:1px solid var(--line);
    background:var(--card2);color:var(--text);padding:9px 11px;font-family:inherit;font-size:12px;resize:vertical;}
  textarea.note:focus{outline:1px solid var(--gold-line);}

  .seg{display:flex;background:var(--card);border:1px solid var(--line);border-radius:12px;padding:3px;margin-bottom:9px;}
  .seg button{flex:1;border:none;background:transparent;color:var(--muted);padding:8px;border-radius:9px;font-size:11.5px;font-weight:600;cursor:pointer;}
  .seg button.on{background:var(--gold-soft);color:var(--gold);}
  .searchbar{display:flex;gap:7px;margin-bottom:9px;}
  .searchbar input,.searchbar select{flex:1;padding:9px 11px;border-radius:11px;border:1px solid var(--line);
    background:var(--card);color:var(--text);font-family:inherit;font-size:12px;}
  .day-card{padding:12px 13px;margin-bottom:8px;cursor:pointer;}
  .day-card.today{border-color:var(--gold-line);background:var(--gold-soft);}
  .dc-top{display:flex;justify-content:space-between;align-items:center;}
  .dc-date{font-weight:600;font-size:12.5px;} .dc-sub{font-size:10px;color:var(--muted);}
  .dc-bar{margin-top:7px;}
  .dc-foot{display:flex;justify-content:space-between;align-items:center;margin-top:7px;font-size:10px;color:var(--muted);}
  .detail-btn{font-size:10px;padding:5px 12px;border-radius:20px;background:var(--gold-soft);color:var(--gold);border:1px solid var(--gold-line);font-weight:600;cursor:pointer;}

  .syl-subject{margin-bottom:8px;overflow:hidden;}
  .syl-head{display:flex;justify-content:space-between;align-items:center;padding:12px 13px;cursor:pointer;}
  .syl-head b{font-size:12px;font-weight:600;} .syl-progress{font-size:10px;color:var(--muted);}
  .syl-topics{display:none;padding:0 13px 9px;} .syl-topics.open{display:block;}
  .syl-item{display:flex;align-items:center;gap:8px;padding:6px 0;font-size:11.5px;border-bottom:1px solid var(--line);}
  .syl-item:last-child{border:none;}
  .syl-item.done span{text-decoration:line-through;color:var(--muted2);}
  .mini-chk{width:16px;height:16px;border-radius:5px;border:1px solid var(--line);flex:none;cursor:pointer;
    display:flex;align-items:center;justify-content:center;font-size:9px;color:transparent;background:var(--card2);}
  .mini-chk.on{background:var(--emerald);color:#08281c;border-color:transparent;}

  .stat-hero{display:flex;flex-direction:column;align-items:center;padding:20px 14px;gap:6px;}
  .stat-hero .ring{width:118px;height:118px;} .stat-hero .pct{font-size:22px;font-weight:700;}
  .grid2{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin:9px 0;}
  .mini-stat{padding:11px;text-align:center;} .mini-stat .v{font-size:16px;font-weight:700;} .mini-stat .l{font-size:9.5px;color:var(--muted);margin-top:2px;}
  .subj-row{display:flex;align-items:center;gap:9px;padding:8px 0;}
  .subj-row .sicon{width:27px;height:27px;border-radius:8px;background:var(--card2);display:flex;align-items:center;justify-content:center;font-size:12px;flex:none;}
  .subj-row .sname{font-size:11px;width:76px;flex:none;color:var(--text);}
  .subj-row .sbar{flex:1;height:6px;border-radius:8px;background:var(--card2);overflow:hidden;}
  .subj-row .sbar>div{height:100%;border-radius:8px;}
  .subj-row .sval{font-size:10px;color:var(--muted);width:42px;text-align:right;flex:none;}
  .week-chart{display:flex;align-items:flex-end;gap:3px;height:78px;padding:10px 5px 0;overflow-x:auto;}
  .week-chart .wcol{flex:0 0 14px;display:flex;flex-direction:column;align-items:center;justify-content:flex-end;height:100%;gap:3px;}
  .week-chart .wbar{width:65%;border-radius:4px 4px 2px 2px;background:linear-gradient(180deg,var(--gold),var(--emerald));min-height:4px;}
  .week-chart .wl{font-size:7.5px;color:var(--muted);}

  .section-title{font-family:'Tiro Bangla',serif;font-size:14px;font-weight:400;margin:16px 3px 8px;color:var(--gold);display:flex;gap:6px;align-items:center;}
  .card-pad{padding:14px 16px;}

  .settings-row{display:flex;justify-content:space-between;align-items:center;padding:10px 2px;border-bottom:1px solid var(--line);font-size:12.5px;}
  .settings-row:last-child{border:none;}
  .switch{width:38px;height:22px;border-radius:20px;background:var(--card2);border:1px solid var(--line);position:relative;cursor:pointer;flex:none;}
  .switch.on{background:var(--gold-soft);border-color:var(--gold-line);}
  .switch::after{content:"";position:absolute;top:2px;left:2px;width:16px;height:16px;border-radius:50%;background:var(--muted);transition:.2s;}
  .switch.on::after{left:18px;background:var(--gold);}
  .btn{padding:9px 13px;border-radius:11px;border:1px solid var(--line);background:var(--card2);color:var(--text);font-family:inherit;font-size:11.5px;font-weight:600;cursor:pointer;}
  .btn:active{opacity:.7;}
  .btn.primary{background:var(--gold-soft);color:var(--gold);border-color:var(--gold-line);}
  .btn.danger{color:#fff;background:var(--danger);border-color:transparent;}
  .btn-row{display:flex;gap:7px;flex-wrap:wrap;margin-top:9px;}
  .chat-msg{max-width:85%;padding:9px 12px;border-radius:14px;margin-bottom:8px;font-size:12.5px;line-height:1.55;}
  .chat-msg.user{background:var(--gold-soft);border:1px solid var(--gold-line);color:var(--text);margin-left:auto;border-bottom-right-radius:4px;}
  .chat-msg.ai{background:var(--card2);border:1px solid var(--line);color:var(--text);margin-right:auto;border-bottom-left-radius:4px;}
  .chat-msg.loading{color:var(--muted);font-style:italic;}
  input[type=date]{padding:8px 9px;border-radius:10px;border:1px solid var(--line);background:var(--card2);color:var(--text);font-family:inherit;}

  .modal-bg{position:fixed;inset:0;background:rgba(0,0,0,0.6);z-index:200;display:none;align-items:flex-end;justify-content:center;}
  .modal-bg.open{display:flex;}
  .modal{width:100%;max-width:520px;border-radius:20px 20px 0 0;padding:18px 17px 24px;max-height:82vh;overflow-y:auto;}
  .modal h3{font-family:'Tiro Bangla',serif;font-weight:400;margin:0 0 8px;font-size:15px;color:var(--gold);}

  footer.credit{text-align:center;font-size:10px;color:var(--muted2);padding:16px 0 6px;font-family:'Tiro Bangla',serif;}

  @media print{
    header.top,.tabs,.icon-btn,.chk,.mini-chk{display:none!important;}
    body{background:#fff;color:#000;} .paper{background:#fff;border:1px solid #ccc;}
  }
  @media (prefers-reduced-motion: reduce){*{animation-duration:.001s!important;transition-duration:.001s!important;}}
</style>
</head>
<body>

<div id="splash">
  <div class="splash-logo">🖍️</div>
  <div class="splash-title">AI স্টাডি প্ল্যানার</div>
  <div class="splash-bar"><span></span></div>
</div>

<div id="app">
  <header class="top">
    <div class="top-row">
      <div class="brand">
        <div class="logo">🖍️</div>
        <div><h1>AI স্টাডি প্ল্যানার</h1><p>২য় মাসিক পরীক্ষা প্রস্তুতি</p></div>
      </div>
      <div style="display:flex;gap:7px;">
        <button class="icon-btn" id="soundBtn">🔊</button>
        <button class="icon-btn" id="themeBtn">🌙</button>
        <button class="icon-btn" id="settingsBtn">⚙️</button>
      </div>
    </div>

    <div class="exam-banner">
      <span>📝 ২য় মাসিক পরীক্ষা</span><span id="examRange">৩–১০ সেপ্টেম্বর</span>
    </div>

    <div class="header-stats">
      <div class="stat-card"><div class="val" id="hdExamCd">--</div><div class="lab">পরীক্ষার বাকি</div></div>
      <div class="stat-card"><div class="val" id="hdStartCd">--</div><div class="lab">রুটিন স্ট্যাটাস</div></div>
      <div class="stat-card"><div class="val" id="hdDone">0/0</div><div class="lab">সেশন</div></div>
      <div class="stat-card"><div class="val" id="hdStreak">🔥 0</div><div class="lab">স্ট্রিক (দিন)</div></div>
    </div>

    <div class="progress-wrap">
      <div class="ring" id="headerRing"></div>
      <div class="progress-info">
        <div class="t1">সিলেবাস অগ্রগতি</div>
        <div class="t2" id="hdPctText">0% সম্পন্ন</div>
        <div class="bar"><div id="hdBar" style="width:0%"></div></div>
      </div>
    </div>

    <div class="tabs">
      <button class="tab-btn active" data-tab="daily">📅 দৈনিক</button>
      <button class="tab-btn" data-tab="overview">📋 প্ল্যান</button>
      <button class="tab-btn" data-tab="stats">📊 স্ট্যাটস</button>
      <button class="tab-btn" data-tab="ai">🤖 AI শিক্ষক</button>
      <button class="tab-btn" data-tab="admin">🛠️ অ্যাডমিন</button>
    </div>
  </header>

  <section class="view active" id="view-daily">
    <div class="paper quote-card"><p id="quoteText">লোড হচ্ছে...</p></div>

    <div class="paper" id="pomodoroCard" style="padding:13px 15px;margin-bottom:10px;">
      <div style="display:flex;justify-content:space-between;align-items:center;">
        <b style="font-size:12.5px;">⏱️ পমোডোরো টাইমার</b>
        <span id="pomoMode" style="font-size:10.5px;color:var(--muted);">পড়ার সময়</span>
      </div>
      <div style="display:flex;align-items:center;gap:14px;margin-top:8px;">
        <div style="font-size:30px;font-weight:800;font-variant-numeric:tabular-nums;" id="pomoClock">২৫:০০</div>
        <div class="btn-row" style="margin:0;flex:1;">
          <button class="btn primary" id="pomoStart">শুরু</button>
          <button class="btn" id="pomoReset">রিসেট</button>
        </div>
      </div>
      <div style="display:flex;align-items:center;gap:10px;margin-top:10px;padding-top:10px;border-top:1px solid var(--line);">
        <label style="font-size:10.5px;color:var(--muted);flex:1;">পড়ার সময় (মিনিট)
          <input type="number" id="pomoStudyMin" min="1" max="180" value="25" style="width:100%;margin-top:3px;padding:6px 8px;border-radius:8px;border:1px solid var(--line);background:var(--card2);color:var(--text);font-size:12px;">
        </label>
        <label style="font-size:10.5px;color:var(--muted);flex:1;">বিরতি (মিনিট)
          <input type="number" id="pomoBreakMin" min="1" max="60" value="5" style="width:100%;margin-top:3px;padding:6px 8px;border-radius:8px;border:1px solid var(--line);background:var(--card2);color:var(--text);font-size:12px;">
        </label>
        <button class="btn" id="pomoApply" style="align-self:flex-end;">✅ সেট করো</button>
      </div>
    </div>

    <div class="paper start-banner" id="startBanner" style="display:none;">
      <b>⏳ রুটিন এখনো শুরু হয়নি</b>
      <p id="startBannerText"></p>
    </div>

    <div class="paper" id="catchupCard" style="display:none;margin-bottom:12px;padding:13px 15px;border:1px solid var(--gold-line);">
      <div style="display:flex;justify-content:space-between;align-items:center;">
        <b style="font-size:12.5px;color:var(--gold);">⏮️ বাদ পড়া টপিক — ধরে ফেলো</b>
        <span id="catchupCount" class="pill" style="background:var(--gold-soft);color:var(--gold);padding:2px 9px;border-radius:20px;font-size:10px;font-weight:700;"></span>
      </div>
      <div class="muted" style="margin-top:4px;font-size:10.5px;">কোনো দিন মিস হলেও টপিক হারিয়ে যায় না — এখান থেকেই যেকোনো সময় শেষ করে ফেলতে পারো।</div>
      <div id="catchupList" style="margin-top:8px;"></div>
    </div>

    <div class="day-title" style="margin:12px 0 4px;">
      <div class="dnum" id="dayNum">Day ১</div>
      <div class="d1" id="dayDate">-</div>
      <div class="d2" id="dayName">-</div>
      <div class="badges" id="dayBadges"></div>
    </div>

    <div class="day-progress-ring"><div class="ring" id="dayRing"></div></div>

    <div id="sessionList"></div>

    <div class="day-nav" style="margin:14px 0;">
      <button id="prevDay">‹ আগের দিন</button>
      <button id="nextDay">পরের দিন ›</button>
    </div>

    <div class="paper" style="margin-top:12px;">
      <div class="prayer-head" id="prayerToggle"><b>🕌 নামাজের সময়</b><span id="prayerArrow">▾</span></div>
      <div class="prayer-list" id="prayerList"></div>
    </div>

    <div class="paper card-pad" style="margin-top:10px;">
      <b style="font-size:12.5px;">📝 নোট</b>
      <textarea class="note" id="dayNote" placeholder="আজকের জন্য নোট লিখুন..."></textarea>
    </div>
  </section>

  <section class="view" id="view-overview">
    <div class="seg">
      <button class="on" data-seg="plan">প্ল্যান</button>
      <button data-seg="syllabus">সিলেবাস</button>
      <button data-seg="difficult">🔥 কঠিন টপিক</button>
    </div>
    <div id="ov-plan">
      <div class="searchbar">
        <input id="searchBox" placeholder="🔍 বিষয় বা টপিক খুঁজুন...">
        <select id="subjectFilter"><option value="">সব বিষয়</option></select>
      </div>
      <div id="dayCards"></div>
    </div>
    <div id="ov-syllabus" style="display:none;"><div id="syllabusList"></div></div>
    <div id="ov-difficult" style="display:none;"><div id="difficultList"></div></div>
  </section>

  <section class="view" id="view-stats">
    <div class="paper stat-hero"><div class="ring" id="statRing"></div><div style="font-size:12px;color:var(--muted);">সার্বিক অগ্রগতি</div></div>
    <div class="grid2">
      <div class="paper mini-stat"><div class="v" id="stCompleted">0</div><div class="l">সম্পন্ন সেশন</div></div>
      <div class="paper mini-stat"><div class="v" id="stRemaining">0</div><div class="l">বাকি সেশন</div></div>
      <div class="paper mini-stat"><div class="v" id="stHours">0</div><div class="l">মোট স্টাডি ঘণ্টা</div></div>
      <div class="paper mini-stat"><div class="v" id="stRevision">0</div><div class="l">রিভিশন সেশন</div></div>
    </div>
    <div class="section-title">📚 বিষয়ভিত্তিক অগ্রগতি</div>
    <div class="paper card-pad" id="subjectStats"></div>
    <div class="section-title">📆 দৈনিক অগ্রগতি</div>
    <div class="paper" id="weekChart"><div class="week-chart" id="weekBars"></div></div>
    <div class="btn-row" style="margin-top:14px;">
      <button class="btn" id="printBtn">🖨️ প্রিন্ট</button>
      <button class="btn" id="exportBtn">⬇️ এক্সপোর্ট</button>
      <button class="btn" id="importBtn">⬆️ ইমপোর্ট</button>
      <input type="file" id="importFile" accept="application/json" style="display:none;">
    </div>
  </section>

  <section class="view" id="view-ai">
    <div class="paper card-pad" id="aiKeyNotice" style="margin-bottom:10px;">
      <b style="font-size:12.5px;">🤖 AI শিক্ষক চালু করতে একটা ফ্রি Gemini API Key লাগবে</b>
      <p style="font-size:11px;color:var(--muted);margin:6px 0 8px;line-height:1.6;">
        <a href="https://aistudio.google.com/app/apikey" target="_blank" style="color:var(--gold);">aistudio.google.com/app/apikey</a> থেকে ফ্রি-তে Key নিয়ে নিচে বসান। Key শুধু আপনার ফোনে (localStorage) সেভ থাকে, কোথাও পাঠানো হয় না — সরাসরি Google-এর সার্ভারে যায়।
      </p>
      <input type="text" id="aiKeyInput" placeholder="আপনার Gemini API Key এখানে বসান" style="width:100%;padding:9px 11px;border-radius:11px;border:1px solid var(--line);background:var(--card2);color:var(--text);font-family:inherit;font-size:12px;margin-bottom:8px;">
      <button class="btn primary" id="aiKeySave" style="width:100%;">সেভ করুন</button>
    </div>

    <div id="aiChatArea" style="display:none;">
      <div class="paper card-pad" style="margin-bottom:10px;">
        <b style="font-size:12.5px;">🤖 তোমার ব্যক্তিগত AI শিক্ষক</b>
        <p style="font-size:10.5px;color:var(--muted);margin:4px 0 0;">যেকোনো টপিক, সমস্যা বা ধারণা নিয়ে জিজ্ঞেস করো — বাংলা বা ইংরেজি দুই ভাষাতেই বলতে পারো।</p>
      </div>
      <div class="btn-row" id="aiSuggestions" style="margin-bottom:6px;">
        <button class="btn" data-q="আজকের পড়ার টপিকগুলো সহজ ভাষায় বুঝিয়ে দাও">আজকের টপিক বুঝিয়ে দাও</button>
        <button class="btn" data-q="আমাকে গণিতের একটা সহজ কৌশল শিখাও">গণিতের কৌশল</button>
        <button class="btn" data-q="একটা ছোট কুইজ নাও আমার শেষ পড়া টপিক থেকে">কুইজ নাও</button>
      </div>
      <div class="paper" id="chatBox" style="padding:12px;min-height:220px;max-height:48vh;overflow-y:auto;margin-bottom:10px;"></div>
      <div style="display:flex;gap:8px;">
        <input type="text" id="aiInput" placeholder="প্রশ্ন লিখো..." style="flex:1;padding:11px 13px;border-radius:12px;border:1px solid var(--line);background:var(--card);color:var(--text);font-family:inherit;font-size:12.5px;">
        <button class="btn primary" id="aiSend">পাঠাও</button>
      </div>
      <div class="btn-row" style="margin-top:8px;">
        <button class="btn" id="aiClear">🗑️ চ্যাট ক্লিয়ার</button>
        <button class="btn" id="aiChangeKey">🔑 Key পরিবর্তন</button>
      </div>
    </div>
  </section>

  <section class="view" id="view-admin">
    <div class="paper card-pad" style="margin-bottom:10px;">
      <b style="font-size:13px;">🛠️ অ্যাডমিন প্যানেল</b>
      <p style="font-size:10.5px;color:var(--muted);margin:4px 0 0;">রুটিনের তারিখ, থিম, সিলেবাস — সবকিছু এখান থেকে নিয়ন্ত্রণ করা যাবে।</p>
    </div>

    <div class="paper card-pad" style="margin-bottom:10px;">
      <div class="section-title" style="margin-top:0;">📅 রুটিনের তারিখ</div>
      <div class="settings-row"><span>রুটিন শুরু</span><input type="date" id="adminStartDate"></div>
      <div class="settings-row"><span>সিলেবাস শেষ</span><input type="date" id="adminEndDate"></div>
      <div class="settings-row"><span>পরীক্ষা শুরু</span><input type="date" id="adminExamStart"></div>
    </div>

    <div class="paper card-pad" style="margin-bottom:10px;">
      <div class="section-title" style="margin-top:0;">🎨 থিম</div>
      <div id="adminThemeSwatches" style="display:flex;gap:8px;flex-wrap:wrap;"></div>
    </div>

    <div class="paper card-pad" style="margin-bottom:10px;">
      <div class="section-title" style="margin-top:0;">📚 সিলেবাস এডিটর</div>
      <p style="font-size:10.5px;color:var(--muted);margin:0 0 8px;">নতুন টপিক/অধ্যায় যোগ, এডিট বা মুছে ফেলা যাবে। পরিবর্তন করলে পুরো রুটিন নতুন করে সাজানো হবে।</p>
      <select id="adminSubjSelect" style="width:100%;margin-bottom:10px;"></select>
      <div id="adminTopicList"></div>
      <div style="display:flex;gap:8px;margin-top:8px;">
        <input type="text" id="adminNewTopic" placeholder="নতুন টপিক লেখো..." style="flex:1;padding:9px 11px;border-radius:10px;border:1px solid var(--line);background:var(--card2);color:var(--text);font-family:inherit;font-size:12px;">
        <button class="btn primary" id="adminAddTopic">➕ যোগ করো</button>
      </div>
    </div>

    <div class="paper card-pad" style="margin-bottom:10px;">
      <div class="section-title" style="margin-top:0;">💾 ডেটা</div>
      <div class="btn-row">
        <button class="btn" id="adminExportBtn">📤 ব্যাকআপ ডাউনলোড</button>
        <button class="btn" id="adminImportBtn">📥 ব্যাকআপ ইমপোর্ট</button>
      </div>
      <div class="btn-row" style="margin-top:8px;">
        <button class="btn danger" id="adminResetBtn">🗑️ প্রোগ্রেস রিসেট</button>
        <button class="btn danger" id="adminResetSyllabusBtn">↩️ সিলেবাস ডিফল্টে ফেরাও</button>
      </div>
    </div>
  </section>

  <footer class="credit">পরিশ্রমই সাফল্যের চাবিকাঠি 🍀</footer>
</div>

<div class="modal-bg" id="settingsModal">
  <div class="paper modal">
    <h3>⚙️ সেটিংস</h3>
    <div class="settings-row"><span>🌙 ডার্ক মোড</span><div class="switch on" id="swTheme"></div></div>
    <div class="settings-row"><span>🔊 সাউন্ড</span><div class="switch on" id="swSound"></div></div>
    <div class="settings-row"><span>🔔 সেশন রিমাইন্ডার</span><div class="switch" id="swReminders"></div></div>
    <div style="font-size:10.5px;color:var(--muted);margin:-4px 0 4px;">* ব্রাউজার ট্যাব খোলা থাকলে কাজ করবে</div>

    <div style="font-size:11.5px;font-weight:700;margin:10px 0 6px;">🎨 থিম বাছাই করো</div>
    <div id="themeSwatches" style="display:flex;gap:8px;flex-wrap:wrap;margin-bottom:10px;"></div>

    <div class="settings-row"><span>📅 রুটিন শুরু</span><input type="date" id="startDateInput"></div>
    <div class="settings-row"><span>📕 সিলেবাস শেষ</span><input type="date" id="endDateInput"></div>
    <div class="settings-row"><span>📝 পরীক্ষা শুরু</span><input type="date" id="examStartInput"></div>
    <div class="btn-row">
      <button class="btn danger" id="resetBtn">🗑️ প্রোগ্রেস রিসেট</button>
      <button class="btn primary" id="closeSettings">বন্ধ করুন</button>
    </div>
  </div>
</div>

<canvas id="confettiCanvas" style="position:fixed;inset:0;pointer-events:none;z-index:300;display:none;"></canvas>

<script>
/* ================= DATA ================= */
const SUBJECTS = [
  {id:'bn1', name:'বাংলা ১ম', icon:'📗', color:'var(--gold)'},
  {id:'bn2', name:'বাংলা ২য়', icon:'📘', color:'var(--amber)'},
  {id:'en1', name:'ইংরেজি ১ম', icon:'📙', color:'var(--sapphire)'},
  {id:'en2', name:'ইংরেজি ২য়', icon:'📕', color:'var(--rose)'},
  {id:'math', name:'গণিত', icon:'🔢', color:'var(--emerald)'},
  {id:'ict', name:'আইসিটি', icon:'💻', color:'var(--violet)'},
  {id:'islam', name:'ইসলাম শিক্ষা', icon:'🕌', color:'var(--teal)'},
  {id:'bgs', name:'বাংলাদেশ ও বিশ্বপরিচয়', icon:'🌏', color:'var(--coral)'},
  {id:'geo', name:'ভূগোল', icon:'🗺️', color:'#e2635f'},
  {id:'sci', name:'বিজ্ঞান', icon:'🔬', color:'#5fc9e2'},
  {id:'econ', name:'অর্থনীতি', icon:'💰', color:'#c589e0'}
];
// এই সাবজেক্টগুলো "কঠিন" ব্যাজ পাবে (গণিত, বাংলাদেশ ও বিশ্বপরিচয়, বিজ্ঞান, ভূগোল)
const HARD_BADGE_SUBJECTS = ['math','bgs','sci','geo'];
const SYLLABUS = {
  bn1:['পল্লী সাহিত্য (গদ্য)','আম-আঁটির ভেঁপু (গদ্য)','মানুষ মুহম্মদ (সা.) (গদ্য)','ওমর ফারুক (পদ্য)','সেইদিন এই মাঠ (পদ্য)','যাব আমি তোমার দেশে (পদ্য)','১৯৭১ (উপন্যাস, পৃ. ৩৪-৫৭)','বহিপীর (নাটক, পৃ. ৮২-৯৩)'],
  bn2:['পরিচ্ছেদ ১৭ ও ১৮: শব্দের শ্রেণিবিভাগ + বিশেষ্য','পরিচ্ছেদ ১৯ ও ২০: সর্বনাম + বিশেষণ','পরিচ্ছেদ ২১, ২২ ও ২৩: ক্রিয়া + ক্রিয়াবিশেষণ + অনুসর্গ',
    'অনুচ্ছেদ: রেলগাড়ি + স্বাধীনতা দিবস','অনুচ্ছেদ: জাতীয় পতাকা + পত্র: আমন্ত্রণপত্র','পত্র: মানপত্র + সারাংশ (পাঠ্যবই ৬)',
    'সারাংশ (পাঠ্যবই ৭) + সারমর্ম (পাঠ্যবই ৬)','সারমর্ম (পাঠ্যবই ৭) + ভাবসম্প্রসারণ (৬)',
    'ভাবসম্প্রসারণ (৭) + ভাবসম্প্রসারণ (১২)','ভাবসম্প্রসারণ (১৩) + প্রবন্ধ: বাংলাদেশের ফুটবল'],
  en1:['Unit Four: Events and Festivals','Unit Six: Our Neighbors','Unit Eleven: Lesson 3 (Poem)','Dialogue Writing','Importance of Learning English','Importance of Reading Newspaper'],
  en2:['Filling Gaps + Right Form of Verbs','Changing Sentences + Completing Sentences','Tag Questions + Narration',
    'Application: Transfer Certificate + Repairing the Damaged Bridge','Application: Seat in the School Hostel + Paragraph: The Life of a Farmer',
    'Paragraph: Load Shedding + A Street Hawker'],
  math:['বীজগণিত: অনুশীলনী ২.২','বীজগণিত: অনুশীলনী ৩.৫','বীজগণিত: অনুশীলনী ১৩.১','ত্রিকোণমিতি: অনুশীলনী ৯.২','পরিমিতি: অনুশীলনী ১৬.২','জ্যামিতি: ৮ম অধ্যায়','পরিসংখ্যান: অনুশীলনী ১৭'],
  ict:['১ম অধ্যায়: তথ্য ও যোগাযোগ প্রযুক্তি ও আমাদের বাংলাদেশ','৫ম অধ্যায়: মাল্টিমিডিয়া ও গ্রাফিক্স'],
  islam:['৩য় অধ্যায়: ইবাদত'],
  bgs:['৫ম অধ্যায়','৬ষ্ঠ অধ্যায়'],
  geo:['৬ষ্ঠ অধ্যায়','৭ম অধ্যায়','৮ম অধ্যায়'],
  sci:['৪র্থ অধ্যায়','১০ম অধ্যায়','১২শ অধ্যায়'],
  econ:['৪র্থ অধ্যায়','৫ম অধ্যায়']
};
const QUOTES = [
"আজকের এক ঘণ্টা পড়াশোনা, আগামীর একটি সুযোগ।","যে পরিশ্রম করে, সে-ই সফলতার মুখ দেখে।",
"ছোট ছোট প্রচেষ্টাই বড় সাফল্যের ভিত্তি।","আজ যা শিখবে, কাল তা-ই তোমার শক্তি।",
"সময়কে সম্মান করো, সময় তোমাকে সম্মান দেবে।","পড়াশোনায় ফাঁকি দিলে ঠকবে নিজেই।",
"লক্ষ্য স্থির থাকলে পথ আপনাই তৈরি হয়।","আজকের কষ্ট আগামীর আরামের চাবিকাঠি।",
"প্রতিদিন একটু একটু করে এগিয়ে যাও, থেমো না।","সফলতার কোনো শর্টকাট নেই, আছে শুধু অধ্যবসায়।",
"তুমি যতটা বিশ্বাস করো, তার চেয়ে বেশি সক্ষম।","আজ যে সময় নষ্ট করবে, কাল তা ফিরে পাবে না।",
"প্রতিটি রিভিশন তোমাকে আরেকটু প্রস্তুত করে তোলে।","নিজের সাথে প্রতিযোগিতা করো, গতকালের চেয়ে ভালো হও।",
"স্বপ্ন দেখো বড়, পরিশ্রম করো তার চেয়েও বেশি।","পড়ায় মনোযোগ দাও, ফলাফল নিজেই কথা বলবে।",
"হার মানার আগে একবার আরেকটু চেষ্টা করো।","আজকের পরিশ্রমই আগামীর পরিচয়।","কষ্ট ছাড়া মেধা বিকশিত হয় না।",
"প্রতিটি অধ্যায় শেষ করা মানে একধাপ এগিয়ে যাওয়া।","নিয়মিত পড়াশোনাই সাফল্যের একমাত্র মন্ত্র।",
"তুমি যা করছো, তা ভবিষ্যতের জন্য বিনিয়োগ।","আজ ফাঁকি দিলে কাল পস্তাতে হবে।",
"লক্ষ্যে অবিচল থাকো, ফলাফল আসবেই।","প্রতিটি চেকবক্স টিক দেওয়া মানে একধাপ এগিয়ে যাওয়া।",
"নিজের সেরাটা দাও, বাকিটা সময়ের হাতে ছেড়ে দাও।","আজকের অধ্যবসায় আগামীর আত্মবিশ্বাস।",
"যে থামে না, সে-ই এগিয়ে যায়।","পরীক্ষার আগের প্রতিটি দিন মূল্যবান, নষ্ট করো না।",
"তোমার পরিশ্রম কেউ দেখুক বা না দেখুক, ফলাফল সবাই দেখবে।","আজ কঠিন মনে হলেও, প্র্যাকটিসেই সহজ হবে।",
"সিলেবাস শেষ করাই আজকের একমাত্র লক্ষ্য হোক।","একটানা পড়ার চেয়ে পরিকল্পিত পড়া বেশি কার্যকর।",
"নিজের সময়সূচি মেনে চলো, ফলাফল নিজেই আসবে।","আজ যতটুকু পড়বে, ততটুকুই তোমার সম্পদ।",
"প্রতিদিনের রুটিন মানাই সফলতার প্রথম ধাপ।","কঠোর পরিশ্রমের বিকল্প কিছু নেই।","আজ হাল ছেড়ো না, লক্ষ্য কাছেই আছে।",
"একাগ্রতা নিয়ে পড়লে সময় কম লাগে, ফল বেশি হয়।","নিজেকে বিশ্বাস করো, তুমি পারবে।",
"রিভিশন ছাড়া প্রস্তুতি অসম্পূর্ণ, নিয়মিত রিভিশন করো।","আজকের প্রতিটি মিনিট পরীক্ষার হলে কাজে দেবে।",
"যে আজ কষ্ট করে, কাল সে-ই হাসে।","পরিকল্পনা করে পড়লে ভয় কেটে যায়।",
"তোমার প্রতিটি প্রচেষ্টা তোমাকে লক্ষ্যের কাছে নিয়ে যাচ্ছে।","নিয়মিততা মেধার চেয়েও বেশি গুরুত্বপূর্ণ।",
"আজ যা কঠিন, নিয়মিত চর্চায় তা সহজ হয়ে যাবে।","সময়ের কাজ সময়ে করো, পরে আফসোস করবে না।",
"প্রতিটি নতুন দিন একটি নতুন সুযোগ, কাজে লাগাও।","তোমার পরিশ্রমই তোমার ভবিষ্যৎ লিখছে।","শেষ মুহূর্তের প্রস্তুতি নয়, নিয়মিত প্রস্তুতিই আসল।"
];
const PRAYERS = [
  {n:'ফজর', i:'🌅', t:'04:10'}, {n:'যোহর', i:'☀️', t:'13:05'}, {n:'আসর', i:'🌤️', t:'16:35'},
  {n:'মাগরিব', i:'🌇', t:'19:12'}, {n:'এশা', i:'🌙', t:'20:30'}
];
const TYPE_LABEL = {study:'স্কুল ডে', holiday:'ছুটির দিন', final:'রিভিশন + মক টেস্ট'};

/* ================= STATE (dates configurable, defaults per plan) ================= */
const STORAGE_KEY = 'studyPlanExam2_v1';
let state = {
  completed:{}, notes:{}, syllabusDone:{}, difficult:{}, currentDayIdx:0, currentTab:'daily',
  theme:'dark', sound:true, reminders:false, apiKey:'', aiChat:[],
  routineStart:'2026-07-20', syllabusEnd:'2026-08-31',
  revisionDays:['2026-09-01','2026-09-02'], examStart:'2026-09-03', examEnd:'2026-09-10'
};
function loadState(){
  try{ const raw = localStorage.getItem(STORAGE_KEY); if(raw) state = Object.assign(state, JSON.parse(raw)); }catch(e){}
}
function saveState(){ try{ localStorage.setItem(STORAGE_KEY, JSON.stringify(state)); }catch(e){} }

/* ================= HELPERS ================= */
const BN_DIGITS=['০','১','২','৩','৪','৫','৬','৭','৮','৯'];
function toBn(n){ return String(n).replace(/[0-9]/g,d=>BN_DIGITS[d]); }
const DAY_NAMES=['রবিবার','সোমবার','মঙ্গলবার','বুধবার','বৃহস্পতিবার','শুক্রবার','শনিবার'];
const MONTH_NAMES=['জানুয়ারি','ফেব্রুয়ারি','মার্চ','এপ্রিল','মে','জুন','জুলাই','আগস্ট','সেপ্টেম্বর','অক্টোবর','নভেম্বর','ডিসেম্বর'];
function dateObj(str){ const [y,m,d]=str.split('-').map(Number); return new Date(y,m-1,d); }
function toISO(d){ return d.getFullYear()+'-'+String(d.getMonth()+1).padStart(2,'0')+'-'+String(d.getDate()).padStart(2,'0'); }
function fmtDate(str){ const d=dateObj(str); return `${toBn(d.getDate())} ${MONTH_NAMES[d.getMonth()]} ${toBn(d.getFullYear())}`; }
function dayName(str){ return DAY_NAMES[dateObj(str).getDay()]; }
function periodLabel(h){
  if(h>=4 && h<6) return 'ভোর'; if(h>=6 && h<12) return 'সকাল'; if(h>=12 && h<15) return 'দুপুর';
  if(h>=15 && h<18) return 'বিকাল'; if(h>=18 && h<20) return 'সন্ধ্যা'; return 'রাত';
}
function fmtRange(start,end){
  const [sh,sm]=start.split(':').map(Number), [eh,em]=end.split(':').map(Number);
  let sh12=sh%12; if(sh12===0) sh12=12; let eh12=eh%12; if(eh12===0) eh12=12;
  const smm = sm===0?'':':'+toBn(String(sm).padStart(2,'0'));
  const emm = em===0?'':':'+toBn(String(em).padStart(2,'0'));
  if(periodLabel(sh)===periodLabel(eh)) return `${periodLabel(sh)} ${toBn(sh12)}${smm}–${toBn(eh12)}${emm}`;
  return `${periodLabel(sh)} ${toBn(sh12)}${smm}–${periodLabel(eh)} ${toBn(eh12)}${emm}`;
}
function todayStr(){ return toISO(new Date()); }
function sessionKey(date,idx){ return date+'_'+idx; }
function subjById(id){ return SUBJECTS.find(s=>s.id===id); }
function getMissedSessions(){
  const today = todayStr();
  const missed = [];
  PLAN.forEach((day, dayIdx)=>{
    if(day.date >= today) return; // আজ বা ভবিষ্যতের দিন বাদ
    day.sessions.forEach((s,i)=>{
      const key = sessionKey(day.date, i);
      if(!state.completed[key]) missed.push({dayIdx, date:day.date, idx:i, session:s, key});
    });
  });
  return missed;
}

/* ================= PLAN GENERATOR ================= */
let PLAN = [];
function buildPlan(){
  const queues = {}; SUBJECTS.forEach(s=>queues[s.id]=SYLLABUS[s.id].map(t=>({subject:s.id,topic:t})));
  const master = [];
  let more = true;
  while(more){ more=false; SUBJECTS.forEach(s=>{ if(queues[s.id].length){ master.push(queues[s.id].shift()); more=true; } }); }

  const start = dateObj(state.routineStart), end = dateObj(state.syllabusEnd);
  // স্কুল ডে (রবি-বৃহস্পতি): স্কুল শেষে বাসায় পড়ার সময় + প্রাইভেট/কোচিং সময়
  // ২/দিন পেসিং — সাধারণ বিষয় ১ ঘণ্টা ১৫ মিনিট, কঠিন (⭐) বিষয় ১ ঘণ্টা ৩০ মিনিট
  const HARD_SUBJECT_IDS = HARD_BADGE_SUBJECTS; // কঠিন-ব্যাজ পাওয়া সাবজেক্টগুলোই বাড়তি সময় পাবে
  function timeToMin(t){ const [h,m]=t.split(':').map(Number); return h*60+m; }
  function minToTime(m){ m=((m%1440)+1440)%1440; const h=Math.floor(m/60), mm=m%60; return String(h).padStart(2,'0')+':'+String(mm).padStart(2,'0'); }
  const SCHOOL_EVENING_START = timeToMin('19:30');
  // শুক্র ও শনি — দুটোই ছুটির দিন, তাই বাড়তি সময় ধরে ৩টা সেশন (সকাল + বিকাল + রাত)
  const WEEKEND_STARTS = [timeToMin('09:30'), timeToMin('14:00'), timeToMin('19:30')];
  const days=[]; let cursor=new Date(start); let qi=0;
  while(cursor<=end){
    const dateStr = toISO(cursor);
    const dow = cursor.getDay(); // ৫=শুক্র, ৬=শনি
    const isWeekend = dow===5 || dow===6;
    const sessionCount = isWeekend ? 3 : 2;
    const sessions=[];
    let schoolCursor = SCHOOL_EVENING_START;
    for(let i=0;i<sessionCount;i++){
      const item = master[qi % master.length];
      const wrapped = qi>=master.length;
      const hard = HARD_SUBJECT_IDS.includes(item.subject) || item.topic.includes(' + ');
      const duration = hard ? 90 : 75;
      let startMin = isWeekend ? WEEKEND_STARTS[i] : schoolCursor;
      const endMin = startMin + duration;
      if(!isWeekend) schoolCursor = endMin;
      const tags = [wrapped?'revision':'new'];
      sessions.push({subject:item.subject, topic:item.topic, start:minToTime(startMin), end:minToTime(endMin), tags});
      qi++;
    }
    days.push({date:dateStr, type: isWeekend?'holiday':'study', sessions});
    cursor.setDate(cursor.getDate()+1);
  }
  state.revisionDays.forEach(dstr=>{
    const sessions = SUBJECTS.map((s,i)=>({subject:s.id, topic:'সম্পূর্ণ রিভিশন + মক টেস্ট', start:SLOTS_R[i%SLOTS_R.length][0], end:SLOTS_R[i%SLOTS_R.length][1], tags:['final']}));
    days.push({date:dstr, type:'final', sessions});
  });
  return days;
}
const SLOTS_R = [['09:00','10:00'],['10:15','11:15'],['15:00','16:00'],['16:15','17:15'],['19:00','20:00'],['20:15','21:00'],['21:15','22:00'],['22:15','23:00']];

function allSessionsFlat(){ const arr=[]; PLAN.forEach(day=>day.sessions.forEach((s,i)=>arr.push({date:day.date, idx:i, ...s}))); return arr; }
function overallProgress(){ const all=allSessionsFlat(); const done=all.filter(s=>state.completed[sessionKey(s.date,s.idx)]).length; return {done,total:all.length,pct: all.length? Math.round(done/all.length*100):0}; }
function computeStreak(){
  // consecutive fully-completed days ending yesterday-or-today, walking backward from today
  const today = todayStr();
  let streak = 0;
  for(let i=PLAN.length-1;i>=0;i--){
    const day = PLAN[i];
    if(day.date > today) continue;
    const allDone = day.sessions.every((s,idx)=>state.completed[sessionKey(day.date,idx)]);
    if(day.date === today && !allDone) continue; // today in progress doesn't break streak
    if(allDone) streak++; else break;
  }
  return streak;
}

/* ================= RING RENDER ================= */
function ringSVG(size,stroke){
  const r=(size-stroke)/2, c=2*Math.PI*r;
  return {c, html:`<svg width="${size}" height="${size}"><circle cx="${size/2}" cy="${size/2}" r="${r}" stroke="rgba(0,0,0,0.08)" stroke-width="${stroke}" fill="none"/>
  <circle class="ringfg" cx="${size/2}" cy="${size/2}" r="${r}" stroke="url(#g2)" stroke-width="${stroke}" fill="none"
  stroke-linecap="round" stroke-dasharray="${c}" stroke-dashoffset="${c}" style="transition:stroke-dashoffset 1s ease;"/>
  <defs><linearGradient id="g2" x1="0%" y1="0%" x2="100%" y2="100%">
  <stop offset="0%" stop-color="#ffe066"/><stop offset="55%" stop-color="#69db7c"/><stop offset="100%" stop-color="#74c0fc"/>
  </linearGradient></defs></svg><div class="pct"></div>`};
}
function mountRing(el,size,stroke,pct){
  const {c,html}=ringSVG(size,stroke); el.innerHTML=html;
  el.querySelector('.pct').textContent = toBn(pct)+'%';
  requestAnimationFrame(()=>requestAnimationFrame(()=>{ el.querySelector('.ringfg').style.strokeDashoffset = c-(c*pct/100); }));
}

/* ================= POMODORO ================= */
function loadPomoDurations(){
  try{
    const saved = JSON.parse(localStorage.getItem('pomoDurations')||'{}');
    return { study: saved.study && saved.study>0 ? saved.study : 25, brk: saved.brk && saved.brk>0 ? saved.brk : 5 };
  }catch(e){ return {study:25, brk:5}; }
}
function savePomoDurations(study, brk){
  try{ localStorage.setItem('pomoDurations', JSON.stringify({study, brk})); }catch(e){}
}
const pomoDur = loadPomoDurations();
let pomo = { seconds: pomoDur.study*60, running:false, mode:'study', timerId:null };
function pomoRender(){
  const m = Math.floor(pomo.seconds/60), s = pomo.seconds%60;
  document.getElementById('pomoClock').textContent = `${toBn(String(m).padStart(2,'0'))}:${toBn(String(s).padStart(2,'0'))}`;
  document.getElementById('pomoMode').textContent = pomo.mode==='study' ? 'পড়ার সময়' : 'বিরতি';
  const fullSeconds = (pomo.mode==='study'?pomoDur.study:pomoDur.brk)*60;
  document.getElementById('pomoStart').textContent = pomo.running ? 'থামুন' : (pomo.seconds < fullSeconds ? 'চালিয়ে যান' : 'শুরু');
}
function pomoTick(){
  if(pomo.seconds>0){ pomo.seconds--; pomoRender(); }
  else{
    playSound();
    if(pomo.mode==='study'){ pomo.mode='break'; pomo.seconds=pomoDur.brk*60; }
    else{ pomo.mode='study'; pomo.seconds=pomoDur.study*60; }
    pomoRender();
  }
}
document.getElementById('pomoStart').addEventListener('click', ()=>{
  pomo.running = !pomo.running;
  if(pomo.running){ pomo.timerId = setInterval(pomoTick, 1000); }
  else{ clearInterval(pomo.timerId); }
  pomoRender();
});
document.getElementById('pomoReset').addEventListener('click', ()=>{
  clearInterval(pomo.timerId); pomo = {seconds:pomoDur.study*60, running:false, mode:'study', timerId:null}; pomoRender();
});
document.getElementById('pomoStudyMin').value = pomoDur.study;
document.getElementById('pomoBreakMin').value = pomoDur.brk;
document.getElementById('pomoApply').addEventListener('click', ()=>{
  let sMin = parseInt(document.getElementById('pomoStudyMin').value, 10);
  let bMin = parseInt(document.getElementById('pomoBreakMin').value, 10);
  if(!sMin || sMin<1) sMin = 25;
  if(!bMin || bMin<1) bMin = 5;
  pomoDur.study = sMin; pomoDur.brk = bMin;
  savePomoDurations(sMin, bMin);
  clearInterval(pomo.timerId);
  pomo = { seconds: (pomo.mode==='study'?sMin:bMin)*60, running:false, mode:pomo.mode, timerId:null };
  pomoRender();
});
pomoRender();


function renderHeader(){
  const {done,total,pct}=overallProgress();
  document.getElementById('hdDone').textContent = `${toBn(done)}/${toBn(total)}`;
  document.getElementById('hdStreak').textContent = `🔥 ${toBn(computeStreak())}`;
  document.getElementById('hdPctText').textContent = `${toBn(pct)}% সম্পন্ন`;
  document.getElementById('hdBar').style.width = pct+'%';
  mountRing(document.getElementById('headerRing'),54,6,pct);

  const now=new Date();
  const examStart = dateObj(state.examStart);
  const examEnd = dateObj(state.examEnd);
  const routineStart = dateObj(state.routineStart);
  const examDiff = Math.ceil((examStart-now)/86400000);
  document.getElementById('hdExamCd').textContent = examDiff>0 ? toBn(examDiff) : (now<=examEnd?'চলছে':'শেষ');

  const startDiff = Math.ceil((routineStart-now)/86400000);
  document.getElementById('hdStartCd').textContent = startDiff>0 ? `${toBn(startDiff)} দিন বাকি` : (now>dateObj(state.syllabusEnd) ? 'সম্পন্ন' : 'চলছে');

  document.getElementById('examRange').textContent = `${fmtDate(state.examStart)} – ${fmtDate(state.examEnd)}`;
  document.getElementById('startDateInput').value = state.routineStart;
  document.getElementById('endDateInput').value = state.syllabusEnd;
  document.getElementById('examStartInput').value = state.examStart;
}

/* ================= DAILY ================= */
function renderDaily(){
  const now = new Date();
  const routineStart = dateObj(state.routineStart);
  const banner = document.getElementById('startBanner');
  if(now < routineStart){
    banner.style.display='block';
    document.getElementById('startBannerText').textContent = `আপনার রুটিন শুরু হবে ${fmtDate(state.routineStart)} থেকে। এখানে Day ১ প্রিভিউ দেখানো হচ্ছে।`;
  } else banner.style.display='none';

  // ⏮️ বাদ পড়া টপিক (আগের দিনের অসম্পূর্ণ সেশন)
  const missed = getMissedSessions();
  const catchupCard = document.getElementById('catchupCard');
  if(missed.length===0){
    catchupCard.style.display='none';
  } else {
    catchupCard.style.display='block';
    document.getElementById('catchupCount').textContent = `${toBn(missed.length)}টা বাকি`;
    document.getElementById('catchupList').innerHTML = missed.slice(0,8).map(m=>{
      const sub = subjById(m.session.subject);
      return `<div style="display:flex;align-items:center;gap:8px;padding:7px 0;border-top:1px solid var(--line);">
        <div class="chk" data-mkey="${m.key}" style="flex:none;">✓</div>
        <div style="flex:1;min-width:0;">
          <div style="font-size:11px;color:${sub?sub.color:'var(--muted)'}">${sub?sub.icon:''} ${sub?sub.name:m.session.subject} · ${fmtDate(m.date)}</div>
          <div style="font-size:11.5px;color:var(--text);white-space:nowrap;overflow:hidden;text-overflow:ellipsis;">${m.session.topic}</div>
        </div>
        <button class="btn btn-sm" data-jump="${m.dayIdx}" style="flex:none;padding:6px 10px;font-size:10px;">দেখো</button>
      </div>`;
    }).join('') + (missed.length>8 ? `<div class="muted" style="margin-top:6px;">+ আরও ${toBn(missed.length-8)}টা বাকি</div>` : '');

    document.getElementById('catchupList').querySelectorAll('.chk').forEach(chk=>{
      chk.addEventListener('click', ()=>{
        const mkey = chk.dataset.mkey;
        state.completed[mkey] = true;
        playSound(); saveState(); renderAll(); checkConfetti();
      });
    });
    document.getElementById('catchupList').querySelectorAll('[data-jump]').forEach(btn=>{
      btn.addEventListener('click', ()=>{
        state.currentDayIdx = parseInt(btn.dataset.jump,10);
        saveState(); renderDaily();
        window.scrollTo({top:0, behavior:'smooth'});
      });
    });
  }

  const day = PLAN[state.currentDayIdx];
  document.getElementById('dayNum').textContent = day.type==='final' ? 'রিভিশন ডে' : `Day ${toBn(state.currentDayIdx+1)}`;
  document.getElementById('dayDate').textContent = fmtDate(day.date);
  document.getElementById('dayName').textContent = dayName(day.date);
  document.getElementById('prevDay').disabled = state.currentDayIdx===0;
  document.getElementById('nextDay').disabled = state.currentDayIdx===PLAN.length-1;

  const badges=[`<span class="badge ${day.type}">${TYPE_LABEL[day.type]}</span>`];
  if(day.date===todayStr()) badges.push(`<span class="badge today">আজকের দিন</span>`);
  document.getElementById('dayBadges').innerHTML = badges.join('');

  const dTotal=day.sessions.length;
  const dDone=day.sessions.filter((s,i)=>state.completed[sessionKey(day.date,i)]).length;
  mountRing(document.getElementById('dayRing'),80,8, dTotal? Math.round(dDone/dTotal*100):0);

  const nowMin = now.getHours()*60+now.getMinutes();
  const list = document.getElementById('sessionList');
  list.innerHTML = day.sessions.map((s,i)=>{
    const sub = subjById(s.subject); const key = sessionKey(day.date,i); const isDone=!!state.completed[key];
    const [sh,sm]=s.start.split(':').map(Number),[eh,em]=s.end.split(':').map(Number);
    const isCurrent = day.date===todayStr() && !isDone && (sh*60+sm)<=nowMin && nowMin<=(eh*60+em);
    const tagLabels = {new:'নতুন', revision:'রিভিশন', private:'প্রাইভেট', final:'ফাইনাল', hard:'কঠিন 🔥'};
    const tagClass = {new:'today', revision:'revision', private:'private', final:'final', hard:'hard'};
    const displayTags = HARD_BADGE_SUBJECTS.includes(s.subject) ? [...s.tags, 'hard'] : s.tags;
    const tagHtml = displayTags.map(t=>`<span class="badge ${tagClass[t]}">${tagLabels[t]}</span>`).join('');
    const dkey = s.subject+'_'+s.topic;
    const isDiff = !!state.difficult[dkey];
    return `<div class="paper session ${isDone?'done':''} ${isCurrent?'current':''}" style="border-left-color:${sub.color}">
      <div class="s-icon">${sub.icon}</div>
      <div class="s-body">
        <div class="s-top"><span class="s-name">${sub.name}</span><span class="s-time">${fmtRange(s.start,s.end)}</span></div>
        <div class="s-topic">${s.topic}</div>
        ${tagHtml?`<div class="s-tags">${tagHtml}</div>`:''}
      </div>
      <div class="s-actions">
        <div class="chk ${isDone?'on':''}" data-key="${key}">${isDone?'✓':''}</div>
        <div class="diffBtn ${isDiff?'on':''}" data-dkey="${dkey}" title="কঠিন টপিক">🔥</div>
        <div class="diffBtn" data-readsubj="${s.subject}" data-readtopic="${s.topic.replace(/"/g,'&quot;')}" title="বই ছাড়া পড়ো">📖</div>
        <div class="diffBtn" data-ytsubj="${s.subject}" data-yttopic="${s.topic.replace(/"/g,'&quot;')}" title="ইউটিউব ভিডিও দেখো">▶️</div>
      </div>
    </div>`;
  }).join('');

  list.querySelectorAll('[data-readsubj]').forEach(btn=>{
    btn.addEventListener('click', ()=>{ readTopicWithAI(btn.dataset.readsubj, btn.dataset.readtopic); });
  });
  list.querySelectorAll('[data-ytsubj]').forEach(btn=>{
    btn.addEventListener('click', ()=>{ openYoutubeSearch(btn.dataset.ytsubj, btn.dataset.yttopic); });
  });

  list.querySelectorAll('.diffBtn').forEach(btn=>{
    btn.addEventListener('click', ()=>{
      const dkey = btn.dataset.dkey;
      state.difficult[dkey] = !state.difficult[dkey];
      saveState(); renderDaily();
    });
  });

  list.querySelectorAll('.chk').forEach(chk=>{
    chk.addEventListener('click', ()=>{
      const key=chk.dataset.key; state.completed[key]=!state.completed[key];
      chk.classList.add('anim'); playSound(); saveState(); renderAll(); checkConfetti();
    });
  });
  document.getElementById('dayNote').value = state.notes[day.date] || '';
}
document.getElementById('dayNote').addEventListener('input', e=>{
  const day = PLAN[state.currentDayIdx]; state.notes[day.date]=e.target.value; saveState();
});
function renderPrayers(){
  document.getElementById('prayerList').innerHTML = PRAYERS.map(p=>`<div class="prayer-item"><div class="pi">${p.i}</div><div class="pn">${p.n}</div><div class="pt">${p.t}</div></div>`).join('');
}
function renderQuote(){ document.getElementById('quoteText').textContent = QUOTES[Math.floor(Math.random()*QUOTES.length)]; }

/* ================= OVERVIEW ================= */
function populateSubjectFilter(){
  document.getElementById('subjectFilter').innerHTML = '<option value="">সব বিষয়</option>' + SUBJECTS.map(s=>`<option value="${s.id}">${s.icon} ${s.name}</option>`).join('');
}
function renderOverview(){
  const q = document.getElementById('searchBox').value.trim().toLowerCase();
  const subjFilter = document.getElementById('subjectFilter').value;
  const cards = PLAN.map((day,dIdx)=>{
    let sessions=day.sessions;
    if(subjFilter) sessions = sessions.filter(s=>s.subject===subjFilter);
    if(q) sessions = sessions.filter(s=> subjById(s.subject).name.toLowerCase().includes(q) || s.topic.toLowerCase().includes(q));
    if((subjFilter||q) && sessions.length===0) return '';
    const total=day.sessions.length, done=day.sessions.filter((s,i)=>state.completed[sessionKey(day.date,i)]).length;
    const pct = total? Math.round(done/total*100):0;
    const isToday = day.date===todayStr();
    const label = day.type==='final' ? 'রিভিশন ডে' : `Day ${toBn(dIdx+1)}`;
    return `<div class="paper day-card ${isToday?'today':''}" data-idx="${dIdx}">
      <div class="dc-top">
        <div><div class="dc-date">${label} · ${fmtDate(day.date)}</div><div class="dc-sub">${dayName(day.date)} · <span class="badge ${day.type}" style="padding:2px 8px;">${TYPE_LABEL[day.type]}</span></div></div>
        <button class="detail-btn">বিস্তারিত</button>
      </div>
      <div class="dc-bar bar"><div style="width:${pct}%"></div></div>
      <div class="dc-foot"><span>${toBn(done)}/${toBn(total)} সেশন সম্পন্ন</span><span>${toBn(pct)}%</span></div>
    </div>`;
  }).filter(Boolean).join('');
  document.getElementById('dayCards').innerHTML = cards || `<div class="paper card-pad" style="text-align:center;color:var(--muted);">কোনো ফলাফল পাওয়া যায়নি</div>`;
  document.querySelectorAll('#dayCards .day-card').forEach(card=>{
    card.addEventListener('click', ()=>{ state.currentDayIdx = Number(card.dataset.idx); switchTab('daily'); saveState(); renderAll(); });
  });
}
function renderSyllabus(){
  document.getElementById('syllabusList').innerHTML = SUBJECTS.map(sub=>{
    const topics = SYLLABUS[sub.id]; const doneCount = topics.filter((t,i)=>state.syllabusDone[sub.id+'_'+i]).length;
    return `<div class="paper syl-subject">
      <div class="syl-head" data-sid="${sub.id}"><b>${sub.icon} ${sub.name} ${HARD_BADGE_SUBJECTS.includes(sub.id)?'<span class="badge hard" style="padding:2px 8px;">কঠিন 🔥</span>':''}</b><span class="syl-progress">${toBn(doneCount)}/${toBn(topics.length)} ▾</span></div>
      <div class="syl-topics" id="syl-${sub.id}">
        ${topics.map((t,i)=>{ const key=sub.id+'_'+i; const done=!!state.syllabusDone[key];
          return `<div class="syl-item ${done?'done':''}"><div class="mini-chk ${done?'on':''}" data-key="${key}">${done?'✓':''}</div><span>${t}</span></div>`; }).join('')}
      </div></div>`;
  }).join('');
  document.querySelectorAll('.syl-head').forEach(h=>h.addEventListener('click', ()=>document.getElementById('syl-'+h.dataset.sid).classList.toggle('open')));
  document.querySelectorAll('.mini-chk').forEach(m=>m.addEventListener('click', e=>{
    e.stopPropagation(); const key=m.dataset.key; state.syllabusDone[key]=!state.syllabusDone[key]; saveState(); renderSyllabus();
  }));
}

function renderDifficult(){
  const marked = Object.keys(state.difficult).filter(k=>state.difficult[k]);
  if(marked.length===0){
    document.getElementById('difficultList').innerHTML = `<div class="paper card-pad" style="text-align:center;color:var(--muted);">এখনো কোনো টপিক "কঠিন" হিসেবে মার্ক করা হয়নি। প্রতিদিনের সেশন কার্ডে 🔥 বাটনে চাপ দিয়ে মার্ক করুন।</div>`;
    return;
  }
  const rows = marked.map(dkey=>{
    const [subId, ...rest] = dkey.split('_'); const topic = rest.join('_');
    const sub = subjById(subId); if(!sub) return '';
    return `<div class="paper session" style="border-left-color:${sub.color};margin-bottom:8px;">
      <div class="s-icon">${sub.icon}</div>
      <div class="s-body"><div class="s-name">${sub.name}</div><div class="s-topic">${topic}</div></div>
      <div class="diffBtn on" data-dkey="${dkey}" title="সরিয়ে ফেলুন">🔥</div>
    </div>`;
  }).join('');
  document.getElementById('difficultList').innerHTML = rows;
  document.querySelectorAll('#difficultList .diffBtn').forEach(btn=>{
    btn.addEventListener('click', ()=>{ state.difficult[btn.dataset.dkey]=false; saveState(); renderDifficult(); });
  });
}

function renderStats(){
  const {done,total,pct} = overallProgress();
  mountRing(document.getElementById('statRing'),120,12,pct);
  document.getElementById('stCompleted').textContent = toBn(done);
  document.getElementById('stRemaining').textContent = toBn(total-done);
  const hours = allSessionsFlat().filter(s=>state.completed[sessionKey(s.date,s.idx)]).reduce((sum,s)=>{
    const [sh,sm]=s.start.split(':').map(Number),[eh,em]=s.end.split(':').map(Number); return sum+((eh*60+em)-(sh*60+sm))/60; },0);
  document.getElementById('stHours').textContent = toBn(Math.round(hours*10)/10);
  const revCount = allSessionsFlat().filter(s=>s.tags.includes('revision')||s.tags.includes('final')).filter(s=>state.completed[sessionKey(s.date,s.idx)]).length;
  document.getElementById('stRevision').textContent = toBn(revCount);

  document.getElementById('subjectStats').innerHTML = SUBJECTS.map(sub=>{
    const sessions = allSessionsFlat().filter(s=>s.subject===sub.id);
    const doneS = sessions.filter(s=>state.completed[sessionKey(s.date,s.idx)]);
    const hrs = doneS.reduce((sum,s)=>{const [sh,sm]=s.start.split(':').map(Number),[eh,em]=s.end.split(':').map(Number);return sum+((eh*60+em)-(sh*60+sm))/60;},0);
    const pctS = sessions.length? Math.round(doneS.length/sessions.length*100):0;
    return `<div class="subj-row"><div class="sicon">${sub.icon}</div><div class="sname">${sub.name}</div>
      <div class="sbar"><div style="width:${pctS}%;background:${sub.color}"></div></div>
      <div class="sval">${toBn(Math.round(hrs*10)/10)}ঘ</div></div>`;
  }).join('');

  document.getElementById('weekBars').innerHTML = PLAN.map((day,i)=>{
    const t=day.sessions.length, d=day.sessions.filter((s,idx)=>state.completed[sessionKey(day.date,idx)]).length;
    const pctD = t? d/t*100:0; const dt=dateObj(day.date);
    return `<div class="wcol"><div class="wbar" style="height:${Math.max(pctD*0.7,4)}%"></div><div class="wl">${toBn(dt.getDate())}</div></div>`;
  }).join('');
}

/* ================= TABS ================= */
function switchTab(tab){
  state.currentTab=tab;
  document.querySelectorAll('.tab-btn').forEach(b=>b.classList.toggle('active', b.dataset.tab===tab));
  document.querySelectorAll('.view').forEach(v=>v.classList.remove('active'));
  document.getElementById('view-'+tab).classList.add('active');
  saveState();
}
document.querySelectorAll('.tab-btn').forEach(b=>b.addEventListener('click', ()=>{ switchTab(b.dataset.tab); renderAll(); if(b.dataset.tab==='ai') aiInitView(); if(b.dataset.tab==='admin') renderAdminPanel(); }));
document.querySelectorAll('.seg button').forEach(b=>{
  b.addEventListener('click', ()=>{
    document.querySelectorAll('.seg button').forEach(x=>x.classList.remove('on')); b.classList.add('on');
    const seg=b.dataset.seg;
    document.getElementById('ov-plan').style.display = seg==='plan'?'block':'none';
    document.getElementById('ov-syllabus').style.display = seg==='syllabus'?'block':'none';
    document.getElementById('ov-difficult').style.display = seg==='difficult'?'block':'none';
    if(seg==='difficult') renderDifficult();
  });
});
document.getElementById('prevDay').addEventListener('click', ()=>{ if(state.currentDayIdx>0){ state.currentDayIdx--; saveState(); renderAll(); } });
document.getElementById('nextDay').addEventListener('click', ()=>{ if(state.currentDayIdx<PLAN.length-1){ state.currentDayIdx++; saveState(); renderAll(); } });
document.getElementById('prayerToggle').addEventListener('click', ()=>{
  document.getElementById('prayerList').classList.toggle('open');
  document.getElementById('prayerArrow').textContent = document.getElementById('prayerList').classList.contains('open') ? '▴' : '▾';
});
document.getElementById('searchBox').addEventListener('input', renderOverview);
document.getElementById('subjectFilter').addEventListener('change', renderOverview);

/* ================= SETTINGS ================= */
const settingsModal = document.getElementById('settingsModal');
document.getElementById('settingsBtn').addEventListener('click', ()=>{ settingsModal.classList.add('open'); renderThemeSwatches(); });
document.getElementById('closeSettings').addEventListener('click', ()=>settingsModal.classList.remove('open'));
settingsModal.addEventListener('click', e=>{ if(e.target===settingsModal) settingsModal.classList.remove('open'); });

const THEME_PRESETS = [
  {id:'dark',   label:'ক্লাসিক গোল্ড', c1:'#0b0d12', c2:'#d8b76a'},
  {id:'light',  label:'হালকা/সাদা',   c1:'#f4f1ea', c2:'#a9822f'},
  {id:'emerald',label:'এমেরাল্ড',     c1:'#081512', c2:'#3fceac'},
  {id:'ocean',  label:'ওশান ব্লু',    c1:'#050f1a', c2:'#4fc3e8'},
  {id:'rose',   label:'রোজ',          c1:'#160a14', c2:'#e88fb0'},
];
function renderThemeSwatches(){
  const el = document.getElementById('themeSwatches');
  el.innerHTML = THEME_PRESETS.map(t=>`
    <div data-theme="${t.id}" style="cursor:pointer;text-align:center;width:56px;">
      <div style="width:44px;height:44px;border-radius:12px;background:${t.c1};border:2px solid ${state.theme===t.id?t.c2:'var(--line)'};
        display:flex;align-items:center;justify-content:center;box-shadow:0 2px 6px rgba(0,0,0,.3);">
        <div style="width:16px;height:16px;border-radius:50%;background:${t.c2};"></div>
      </div>
      <div style="font-size:9px;color:var(--muted);margin-top:4px;">${t.label}</div>
    </div>`).join('');
  el.querySelectorAll('[data-theme]').forEach(sw=>{
    sw.addEventListener('click', ()=>{ state.theme = sw.dataset.theme; applyTheme(); saveState(); renderThemeSwatches(); });
  });
}
function applyTheme(){
  document.body.classList.remove('light','theme-emerald','theme-ocean','theme-rose');
  if(state.theme==='light') document.body.classList.add('light');
  else if(state.theme==='emerald') document.body.classList.add('theme-emerald');
  else if(state.theme==='ocean') document.body.classList.add('theme-ocean');
  else if(state.theme==='rose') document.body.classList.add('theme-rose');
  const isDarkFamily = state.theme!=='light';
  document.getElementById('themeBtn').textContent = state.theme==='light' ? '☀️' : '🌙';
  document.getElementById('swTheme').classList.toggle('on', isDarkFamily);
}
document.getElementById('themeBtn').addEventListener('click', ()=>{ state.theme = state.theme==='light'?'dark':'light'; applyTheme(); saveState(); renderThemeSwatches(); });
document.getElementById('swTheme').addEventListener('click', ()=>{ state.theme = state.theme==='light'?'dark':'light'; applyTheme(); saveState(); renderThemeSwatches(); });
function applySound(){ document.getElementById('soundBtn').textContent = state.sound?'🔊':'🔇'; document.getElementById('swSound').classList.toggle('on', state.sound); }
document.getElementById('soundBtn').addEventListener('click', ()=>{ state.sound=!state.sound; applySound(); saveState(); });
document.getElementById('swSound').addEventListener('click', ()=>{ state.sound=!state.sound; applySound(); saveState(); });

function applyReminders(){ document.getElementById('swReminders').classList.toggle('on', state.reminders); }
document.getElementById('swReminders').addEventListener('click', async ()=>{
  if(!state.reminders){
    if(!('Notification' in window)){ alert('আপনার ব্রাউজার নোটিফিকেশন সাপোর্ট করে না।'); return; }
    const perm = await Notification.requestPermission();
    if(perm!=='granted'){ alert('নোটিফিকেশনের অনুমতি দেওয়া হয়নি।'); return; }
  }
  state.reminders = !state.reminders; applyReminders(); saveState();
});
let _notifiedKeys = {};
function checkReminders(){
  if(!state.reminders || !('Notification' in window) || Notification.permission!=='granted') return;
  const today = todayStr();
  const day = PLAN.find(d=>d.date===today);
  if(!day) return;
  const now = new Date(); const nowMin = now.getHours()*60+now.getMinutes();
  day.sessions.forEach((s,i)=>{
    const key = sessionKey(today,i);
    const [sh,sm] = s.start.split(':').map(Number);
    if(sh*60+sm===nowMin && !_notifiedKeys[key] && !state.completed[key]){
      _notifiedKeys[key]=true;
      const sub = subjById(s.subject);
      try{ new Notification('📖 পড়ার সময় হয়েছে!', {body:`${sub.name} — ${s.topic}`}); }catch(e){}
    }
  });
}
setInterval(checkReminders, 20000);

function playSound(){
  if(!state.sound) return;
  try{ const ctx=new (window.AudioContext||window.webkitAudioContext)(); const o=ctx.createOscillator(); const g=ctx.createGain();
    o.type='sine'; o.frequency.value=880; g.gain.value=0.06; o.connect(g); g.connect(ctx.destination); o.start();
    setTimeout(()=>{o.stop(); ctx.close();},130); }catch(e){}
}
document.getElementById('startDateInput').addEventListener('change', e=>{ state.routineStart=e.target.value; PLAN=buildPlan(); saveState(); renderAll(); });
document.getElementById('endDateInput').addEventListener('change', e=>{ state.syllabusEnd=e.target.value; PLAN=buildPlan(); saveState(); renderAll(); });
document.getElementById('examStartInput').addEventListener('change', e=>{ state.examStart=e.target.value; saveState(); renderHeader(); });

document.getElementById('resetBtn').addEventListener('click', ()=>{
  if(confirm('আপনি কি নিশ্চিতভাবে সব প্রোগ্রেস রিসেট করতে চান? এই কাজটি ফিরিয়ে নেওয়া যাবে না।')){
    state.completed={}; state.notes={}; state.syllabusDone={}; saveState(); renderAll(); settingsModal.classList.remove('open');
  }
});
document.getElementById('exportBtn').addEventListener('click', ()=>{
  const blob = new Blob([JSON.stringify(state,null,2)], {type:'application/json'});
  const url = URL.createObjectURL(blob); const a=document.createElement('a'); a.href=url; a.download='study-plan-exam2-progress.json'; a.click(); URL.revokeObjectURL(url);
});
document.getElementById('importBtn').addEventListener('click', ()=>document.getElementById('importFile').click());
document.getElementById('importFile').addEventListener('change', e=>{
  const file=e.target.files[0]; if(!file) return;
  const reader=new FileReader();
  reader.onload = ()=>{ try{ const imported=JSON.parse(reader.result); state=Object.assign(state, imported); applyCustomSyllabus(); PLAN=buildPlan(); saveState(); applyTheme(); applySound(); renderAll(); alert('ইমপোর্ট সম্পন্ন হয়েছে!'); }catch(err){ alert('ফাইলটি সঠিক নয়।'); } };
  reader.readAsText(file);
});
document.getElementById('printBtn').addEventListener('click', ()=>window.print());

/* ================= CONFETTI ================= */
function checkConfetti(){ const {pct}=overallProgress(); if(pct===100) fireConfetti(); }
function fireConfetti(){
  const canvas=document.getElementById('confettiCanvas'); canvas.style.display='block';
  canvas.width=window.innerWidth; canvas.height=window.innerHeight;
  const ctx=canvas.getContext('2d'); const colors=['#ffe066','#ff8fa3','#74c0fc','#69db7c','#ffa94d','#c39bd3'];
  let pieces=Array.from({length:120},()=>({x:Math.random()*canvas.width,y:-20-Math.random()*canvas.height*0.5,r:4+Math.random()*5,
    c:colors[Math.floor(Math.random()*colors.length)],vy:2+Math.random()*3,vx:-1+Math.random()*2,rot:Math.random()*360}));
  let frames=0;
  function loop(){
    ctx.clearRect(0,0,canvas.width,canvas.height);
    pieces.forEach(p=>{ p.y+=p.vy;p.x+=p.vx;p.rot+=5; ctx.save(); ctx.translate(p.x,p.y); ctx.rotate(p.rot*Math.PI/180);
      ctx.fillStyle=p.c; ctx.fillRect(-p.r/2,-p.r/2,p.r,p.r*1.6); ctx.restore(); });
    frames++;
    if(frames<160) requestAnimationFrame(loop); else { canvas.style.display='none'; ctx.clearRect(0,0,canvas.width,canvas.height); }
  }
  loop();
}

/* ================= AI TEACHER (Gemini) ================= */
function openYoutubeSearch(subjId, topic){
  const sub = subjById(subjId);
  const subjName = sub ? sub.name : subjId;
  const query = `${subjName} ${topic} ৯ম শ্রেণি ক্লাস ব্যাখ্যা`;
  const url = 'https://www.youtube.com/results?search_query=' + encodeURIComponent(query);
  window.open(url, '_blank');
}
function readTopicWithAI(subjId, topic){
  const sub = subjById(subjId);
  switchTab('ai'); saveState(); renderAll(); aiInitView();
  if(!state.apiKey){
    window.scrollTo({top:0, behavior:'smooth'});
    return; // key notice দেখাবে, key দিলে নিচের প্রশ্নটা ব্যবহারকারী নিজে থেকে জিজ্ঞেস করতে পারবে
  }
  document.getElementById('aiInput').value =
    `আমার কাছে বই নেই, তাই "${sub?sub.name:subjId}" বিষয়ের "${topic}" — এই টপিকটা মোবাইলেই পড়তে চাই। দয়া করে বইয়ের মতো করে বিস্তারিত ব্যাখ্যা করো: মূল বিষয়বস্তু, গুরুত্বপূর্ণ পয়েন্ট, উদাহরণ এবং পরীক্ষায় যা লিখতে হবে — সবকিছু সহজ ভাষায়।`;
  aiSend();
}
function aiInitView(){
  const hasKey = !!state.apiKey;
  document.getElementById('aiKeyNotice').style.display = hasKey ? 'none' : 'block';
  document.getElementById('aiChatArea').style.display = hasKey ? 'block' : 'none';
  if(hasKey) aiRenderChat();
}
document.getElementById('aiKeySave').addEventListener('click', ()=>{
  const val = document.getElementById('aiKeyInput').value.trim();
  if(!val){ alert('একটা API Key বসান।'); return; }
  state.apiKey = val; saveState(); aiInitView();
});
document.getElementById('aiChangeKey').addEventListener('click', ()=>{
  if(confirm('API Key পরিবর্তন করতে চান?')){ state.apiKey=''; saveState(); aiInitView(); }
});
document.getElementById('aiClear').addEventListener('click', ()=>{
  if(confirm('পুরো চ্যাট মুছে ফেলতে চান?')){ state.aiChat=[]; saveState(); aiRenderChat(); }
});
document.querySelectorAll('#aiSuggestions .btn').forEach(b=>{
  b.addEventListener('click', ()=>{ document.getElementById('aiInput').value = b.dataset.q; aiSend(); });
});
document.getElementById('aiSend').addEventListener('click', aiSend);
document.getElementById('aiInput').addEventListener('keydown', e=>{ if(e.key==='Enter') aiSend(); });

function aiRenderChat(){
  const box = document.getElementById('chatBox');
  if(state.aiChat.length===0){
    box.innerHTML = `<div style="text-align:center;color:var(--muted);font-size:12px;padding:20px 10px;">নমস্কার! আমি তোমার AI শিক্ষক। যেকোনো টপিক নিয়ে জিজ্ঞেস করো 🌟</div>`;
    return;
  }
  box.innerHTML = state.aiChat.map(m=>`<div class="chat-msg ${m.role==='user'?'user':'ai'}">${m.role==='user'?escapeHtml(m.text).replace(/\n/g,'<br>'):formatAiText(m.text)}</div>`).join('');
  box.scrollTop = box.scrollHeight;
}
function formatAiText(str){
  // হালকা markdown রেন্ডার: **বোল্ড**, - বুলেট পয়েন্ট, নম্বরযুক্ত লিস্ট — চ্যাট আরও গোছানো দেখানোর জন্য
  let s = escapeHtml(str);
  s = s.replace(/\*\*(.+?)\*\*/g, '<b>$1</b>');
  const lines = s.split('\n');
  let html = ''; let inList = false;
  lines.forEach(line=>{
    const bullet = line.match(/^\s*[-•]\s+(.*)/);
    const numbered = line.match(/^\s*(\d+)[.)]\s+(.*)/);
    if(bullet){
      if(!inList){ html += '<ul style="margin:4px 0 4px 18px;padding:0;">'; inList='ul'; }
      html += `<li style="margin-bottom:3px;">${bullet[1]}</li>`;
    } else if(numbered){
      if(!inList){ html += '<ol style="margin:4px 0 4px 18px;padding:0;">'; inList='ol'; }
      html += `<li style="margin-bottom:3px;">${numbered[2]}</li>`;
    } else {
      if(inList){ html += inList==='ul'?'</ul>':'</ol>'; inList=false; }
      if(line.trim()==='') html += '<br>'; else html += line + '<br>';
    }
  });
  if(inList) html += inList==='ul'?'</ul>':'</ol>';
  return html;
}
function escapeHtml(str){ const d=document.createElement('div'); d.textContent=str; return d.innerHTML; }

function aiBuildContext(){
  const day = PLAN[state.currentDayIdx];
  const topics = day ? day.sessions.map(s=>`${subjById(s.subject).name}: ${s.topic}`).join(', ') : '';
  return `তুমি একজন অভিজ্ঞ, ধৈর্যশীল ও উৎসাহদায়ক AI শিক্ষক। তুমি একজন বাংলাদেশি ৯ম শ্রেণির মানবিক বিভাগের ছাত্র/ছাত্রীকে "২য় মাসিক পরীক্ষা"-র জন্য পড়াচ্ছো। বিষয়সমূহ: বাংলা ১ম ও ২য়, ইংরেজি ১ম ও ২য়, গণিত, আইসিটি, ইসলাম শিক্ষা, বাংলাদেশ ও বিশ্বপরিচয়, ভূগোল, বিজ্ঞান, অর্থনীতি। পরীক্ষা ${fmtDate(state.examStart)} থেকে ${fmtDate(state.examEnd)}। আজকের (Day ${state.currentDayIdx+1}) পরিকল্পিত টপিক: ${topics || 'নেই'}।

উত্তরের মান নিয়ে কড়াভাবে মেনে চলবে এই নিয়মগুলো:
- ভাষা: সহজ, শুদ্ধ, প্রাঞ্জল বাংলা (ছাত্র ইংরেজিতে জিজ্ঞেস করলে ইংরেজিতে উত্তর দাও)। বানান ও ব্যাকরণ নিখুঁত রাখবে।
- গঠন: ছোট ছোট অনুচ্ছেদ ব্যবহার করবে, প্রয়োজনে **বোল্ড** দিয়ে গুরুত্বপূর্ণ শব্দ/সংজ্ঞা হাইলাইট করবে, ধাপে ধাপে বুলেট বা নম্বরযুক্ত পয়েন্ট দিয়ে সাজাবে — কখনো একটানা বড় প্যারাগ্রাফ লিখবে না।
- গভীরতা: শুধু তথ্য না দিয়ে "কেন" ও "কীভাবে" বুঝিয়ে দেবে, বাস্তব বা সহজবোধ্য উদাহরণ/উপমা দিয়ে বিষয়টা প্রাণবন্ত করে তুলবে।
- পরীক্ষা-উপযোগিতা: প্রাসঙ্গিক হলে বলে দেবে বোর্ড পরীক্ষায় এই অংশ থেকে কী ধরনের প্রশ্ন আসতে পারে এবং উত্তরে ঠিক কী কী পয়েন্ট থাকা দরকার।
- নির্ভুলতা: অনিশ্চিত থাকলে অনুমান করে ভুল তথ্য দেবে না, বরং বলবে পাঠ্যবইয়ের সাথে মিলিয়ে নিতে।
- দৈর্ঘ্য: মোবাইল স্ক্রিনে পড়তে হবে, তাই উত্তর প্রয়োজন অনুযায়ী পূর্ণাঙ্গ কিন্তু অপ্রয়োজনীয় বাহুল্য ছাড়া — প্রতিটা লাইন যেন মূল্য যোগ করে।
- সুর: উৎসাহব্যঞ্জক ও বন্ধুত্বপূর্ণ, কখনো রূঢ় বা যান্ত্রিক শোনাবে না।`;
}

async function aiSend(){
  const input = document.getElementById('aiInput');
  const text = input.value.trim();
  if(!text) return;
  if(!state.apiKey){ aiInitView(); return; }
  state.aiChat.push({role:'user', text});
  input.value='';
  aiRenderChat(); saveState();

  const box = document.getElementById('chatBox');
  box.insertAdjacentHTML('beforeend', `<div class="chat-msg ai loading" id="aiLoadingMsg">শিক্ষক লিখছেন...</div>`);
  box.scrollTop = box.scrollHeight;

  try{
    const contents = state.aiChat.slice(-16).map(m=>({role: m.role==='user'?'user':'model', parts:[{text:m.text}]}));
    const res = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-flash-latest:generateContent?key=${encodeURIComponent(state.apiKey)}`, {
      method:'POST', headers:{'Content-Type':'application/json'},
      body: JSON.stringify({ systemInstruction:{parts:[{text: aiBuildContext()}]}, contents })
    });
    const data = await res.json();
    const loadingEl = document.getElementById('aiLoadingMsg'); if(loadingEl) loadingEl.remove();
    if(!res.ok){
      const msg = data?.error?.message || 'অজানা সমস্যা হয়েছে।';
      state.aiChat.push({role:'ai', text:`⚠️ সমস্যা হয়েছে: ${msg}\n\nAPI Key ঠিক আছে কিনা চেক করুন, অথবা কিছুক্ষণ পর আবার চেষ্টা করুন।`});
    } else {
      const reply = data?.candidates?.[0]?.content?.parts?.map(p=>p.text).join('') || 'দুঃখিত, উত্তর তৈরি করতে পারিনি। আবার চেষ্টা করো।';
      state.aiChat.push({role:'ai', text:reply});
      playSound();
    }
  }catch(e){
    const loadingEl = document.getElementById('aiLoadingMsg'); if(loadingEl) loadingEl.remove();
    state.aiChat.push({role:'ai', text:'⚠️ ইন্টারনেট কানেকশন বা সার্ভারে সমস্যা হয়েছে, আবার চেষ্টা করো।'});
  }
  aiRenderChat(); saveState();
}


function renderAll(){
  renderHeader(); renderDaily(); renderPrayers();
  if(document.getElementById('view-overview').classList.contains('active')){ renderOverview(); renderSyllabus(); }
  if(document.getElementById('view-stats').classList.contains('active')) renderStats();
}

/* ================= ADMIN PANEL ================= */
function renderAdminPanel(){
  document.getElementById('adminStartDate').value = state.routineStart;
  document.getElementById('adminEndDate').value = state.syllabusEnd;
  document.getElementById('adminExamStart').value = state.examStart;

  const themeBox = document.getElementById('adminThemeSwatches');
  themeBox.innerHTML = THEME_PRESETS.map(t=>`
    <div data-theme="${t.id}" style="cursor:pointer;text-align:center;width:56px;">
      <div style="width:44px;height:44px;border-radius:12px;background:${t.c1};border:2px solid ${state.theme===t.id?t.c2:'var(--line)'};
        display:flex;align-items:center;justify-content:center;box-shadow:0 2px 6px rgba(0,0,0,.3);">
        <div style="width:16px;height:16px;border-radius:50%;background:${t.c2};"></div>
      </div>
      <div style="font-size:9px;color:var(--muted);margin-top:4px;">${t.label}</div>
    </div>`).join('');
  themeBox.querySelectorAll('[data-theme]').forEach(sw=>{
    sw.addEventListener('click', ()=>{ state.theme = sw.dataset.theme; applyTheme(); saveState(); renderAdminPanel(); });
  });

  const sel = document.getElementById('adminSubjSelect');
  const prevVal = sel.value || SUBJECTS[0].id;
  sel.innerHTML = SUBJECTS.map(s=>`<option value="${s.id}" ${s.id===prevVal?'selected':''}>${s.icon} ${s.name}</option>`).join('');
  renderAdminTopicList();
}
function renderAdminTopicList(){
  const subjId = document.getElementById('adminSubjSelect').value;
  const list = document.getElementById('adminTopicList');
  const topics = SYLLABUS[subjId] || [];
  list.innerHTML = topics.map((t,i)=>`
    <div style="display:flex;gap:6px;align-items:center;padding:6px 0;border-top:1px solid var(--line);">
      <input type="text" data-editidx="${i}" value="${t.replace(/"/g,'&quot;')}" style="flex:1;padding:7px 9px;border-radius:8px;border:1px solid var(--line);background:var(--card2);color:var(--text);font-family:inherit;font-size:11.5px;">
      <button class="diffBtn" data-delidx="${i}" style="color:var(--danger);flex:none;">🗑️</button>
    </div>`).join('') || `<div class="muted" style="padding:8px 0;">এই বিষয়ে এখনো কোনো টপিক নেই।</div>`;

  list.querySelectorAll('[data-editidx]').forEach(inp=>{
    inp.addEventListener('change', ()=>{
      const idx = parseInt(inp.dataset.editidx,10);
      SYLLABUS[subjId][idx] = inp.value.trim() || SYLLABUS[subjId][idx];
      persistSyllabusChange();
    });
  });
  list.querySelectorAll('[data-delidx]').forEach(btn=>{
    btn.addEventListener('click', ()=>{
      const idx = parseInt(btn.dataset.delidx,10);
      if(!confirm('এই টপিকটা মুছে ফেলবে?')) return;
      SYLLABUS[subjId].splice(idx,1);
      persistSyllabusChange();
      renderAdminTopicList();
    });
  });
}
function persistSyllabusChange(){
  state.customSyllabus = JSON.parse(JSON.stringify(SYLLABUS));
  PLAN = buildPlan();
  saveState();
  renderOverview(); renderSyllabus(); renderAll();
}
document.getElementById('adminSubjSelect').addEventListener('change', renderAdminTopicList);
document.getElementById('adminAddTopic').addEventListener('click', ()=>{
  const subjId = document.getElementById('adminSubjSelect').value;
  const input = document.getElementById('adminNewTopic');
  const val = input.value.trim();
  if(!val) return;
  SYLLABUS[subjId].push(val);
  input.value='';
  persistSyllabusChange();
  renderAdminTopicList();
});
document.getElementById('adminStartDate').addEventListener('change', e=>{ state.routineStart=e.target.value; PLAN=buildPlan(); saveState(); renderAll(); });
document.getElementById('adminEndDate').addEventListener('change', e=>{ state.syllabusEnd=e.target.value; PLAN=buildPlan(); saveState(); renderAll(); });
document.getElementById('adminExamStart').addEventListener('change', e=>{ state.examStart=e.target.value; saveState(); renderHeader(); });
document.getElementById('adminExportBtn').addEventListener('click', ()=>document.getElementById('exportBtn').click());
document.getElementById('adminImportBtn').addEventListener('click', ()=>document.getElementById('importBtn').click());
document.getElementById('adminResetBtn').addEventListener('click', ()=>document.getElementById('resetBtn').click());
document.getElementById('adminResetSyllabusBtn').addEventListener('click', ()=>{
  if(!confirm('সিলেবাস ডিফল্ট (মূল) অবস্থায় ফিরিয়ে দেবে, তোমার করা সব পরিবর্তন হারিয়ে যাবে। নিশ্চিত?')) return;
  Object.keys(DEFAULT_SYLLABUS).forEach(k=>{ SYLLABUS[k] = DEFAULT_SYLLABUS[k].slice(); });
  delete state.customSyllabus;
  PLAN = buildPlan(); saveState();
  renderAdminPanel(); renderOverview(); renderSyllabus(); renderAll();
});

/* ================= INIT ================= */
const DEFAULT_SYLLABUS = JSON.parse(JSON.stringify(SYLLABUS));
function applyCustomSyllabus(){
  if(state.customSyllabus){
    Object.keys(state.customSyllabus).forEach(k=>{ if(SYLLABUS[k]) SYLLABUS[k] = state.customSyllabus[k].slice(); });
  }
}
function init(){
  loadState();
  applyCustomSyllabus();
  PLAN = buildPlan();
  populateSubjectFilter();
  applyTheme(); applySound(); applyReminders(); renderQuote();

  const today = todayStr();
  let idx = PLAN.findIndex(d=>d.date===today);
  if(idx<0){
    const now = new Date();
    if(now < dateObj(state.routineStart)) idx = 0;
    else idx = PLAN.length-1;
  }
  state.currentDayIdx = idx;
  switchTab(state.currentTab || 'daily');
  renderOverview(); renderSyllabus(); renderAll();

  setTimeout(()=>document.getElementById('splash').classList.add('hide'), 1100);
  setInterval(renderHeader, 60000);
}
init();
</script>
</body>
</html>
